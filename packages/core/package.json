{
  "name": "core",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "shared": "workspace:*"
  },
  "devDependencies": {
    "asr-browser": "workspace:*",
    "translator": "workspace:*"
  }
}
