# 任务计划书.md

# Browser-native Live Translator 基础功能任务计划书

## 1. 当前阶段目标

本阶段目标是制作一个**完全基于浏览器运行**的实时直播翻译插件基础版本。

第一版重点验证核心链路，而不是追求完整多语言覆盖。

基础版本暂定支持：

- 识别语言：普通话 / English / Japanese
- 翻译目标语言：中文 / English
- 识别语言由用户手动选择
- 翻译目标语言由用户手动选择
- 暂不支持自动语言检测
- 暂不支持用户术语表
- 暂不支持韩语识别与韩语输出
- 暂不支持日文输出
- 暂不支持本地 Server / Docker / Python ASR
- 暂不支持云端 ASR

核心链路：

```text
当前标签页音频
  ↓
浏览器端 ASR
  ↓
用户选择的源语言文本
  ↓
OpenAI-compatible LLM API
  ↓
用户选择的目标语言字幕
  ↓
页面 Overlay 显示
```

---

## 2. 支持语言范围

## 2.1 识别语言

第一阶段支持用户手动选择以下 ASR 源语言：

| 显示名称 | 语言代码 | 说明 |
|---|---|---|
| Mandarin Chinese | `zh` | 普通话 |
| English | `en` | 英语 |
| Japanese | `ja` | 日语 |

暂不支持：

- 自动语言检测
- 韩语识别
- 多语言混说识别
- 其他语言扩展

---

## 2.2 翻译目标语言

第一阶段只支持以下翻译目标语言：

| 显示名称 | 语言代码 | 说明 |
|---|---|---|
| Chinese | `zh-CN` | 简体中文 |
| English | `en` | 英文 |

默认配置：

```text
sourceLang = ja
targetLang = zh-CN
```

这样做的原因：

- 中文和英文排版更容易控制。
- 用户可以直接判断翻译质量。
- 先避免日文 / 韩文输出的字体、换行、质量评估问题。
- 更适合作为第一版 MVP。

---

## 3. 功能模块拆分

## 3.1 Web Demo

Web Demo 用于在不涉及 Chrome Extension 权限的情况下验证核心链路。

### 任务 3.1.1 创建 Web Demo 项目

目标：

- 使用 Vite + React + TypeScript。
- 提供最小页面。
- 能运行 `pnpm dev`。

交付物：

```text
apps/web-demo/
  index.html
  vite.config.ts
  src/
    main.tsx
    App.tsx
```

验收标准：

- 页面能正常启动。
- 页面显示基础标题和状态。

---

### 任务 3.1.2 实现语言选择 UI

目标：

- 用户可以选择识别语言。
- 用户可以选择翻译目标语言。
- 默认值为 `ja → zh-CN`。

交付物：

```text
LanguageSelector.tsx
```

验收标准：

- Source Language 下拉框只包含：
  - Mandarin Chinese
  - English
  - Japanese
- Target Language 下拉框只包含：
  - Chinese
  - English
- 选择结果能写入应用状态。

---

### 任务 3.1.3 实现音频上传测试

目标：

- 用户可以上传本地音频文件。
- 页面可以读取音频文件并传给后续 pipeline。

交付物：

```text
AudioUploader.tsx
```

验收标准：

- 用户选择音频文件后，页面显示文件名。
- 能将音频文件转换为 ASR 模块可处理的数据。

---

### 任务 3.1.4 实现 Mock ASR

目标：

- 暂不接真实 ASR。
- 根据用户选择的 sourceLang 输出假识别文本。

示例：

```text
en: Hello everyone, today we are playing Minecraft.
zh: 大家好，今天我们来玩 Minecraft。
ja: 今日はマイクラをやります。
```

交付物：

```text
packages/asr-browser/src/MockASRProvider.ts
```

验收标准：

- 用户选择不同 sourceLang 时，Mock ASR 输出对应语言文本。
- 不依赖真实模型。

---

### 任务 3.1.5 实现 Mock Translator

目标：

- 暂不调用真实 LLM API。
- 根据 targetLang 输出假翻译文本。

交付物：

```text
packages/translator/src/MockTranslator.ts
```

验收标准：

- pipeline 可以从 Mock ASR 走到 Mock Translator。
- 页面能显示 sourceText 和 translatedText。

---

### 任务 3.1.6 实现 OpenAI-compatible Translator

目标：

- 支持用户输入：
  - API Base URL
  - API Key
  - Model Name
- 使用 OpenAI-compatible Chat Completions 格式调用 LLM。
- 只要求翻译到 `zh-CN` 或 `en`。

交付物：

```text
packages/translator/src/OpenAICompatibleTranslator.ts
packages/translator/src/prompt.ts
```

验收标准：

- 用户填写 API 配置后，可以调用兼容接口。
- 能把 sourceText 翻译成中文或英文。
- API Key 不写死在代码里。

---

### 任务 3.1.7 实现 Web Demo 字幕预览

目标：

- 页面显示识别文本。
- 页面显示翻译文本。
- 显示当前 sourceLang / targetLang。

交付物：

```text
SubtitlePreview.tsx
```

验收标准：

- 可以完整展示：
  - 源语言文本
  - 目标语言文本
  - 当前状态

---

## 3.2 核心 Pipeline

核心 Pipeline 不依赖 Chrome Extension API，应可被 Web Demo 和 Extension 复用。

### 任务 3.2.1 定义共享类型

目标：

定义基础类型。

交付物：

```text
packages/shared/src/language.ts
packages/shared/src/audio.ts
packages/shared/src/asr.ts
packages/shared/src/translation.ts
packages/shared/src/subtitle.ts
packages/shared/src/settings.ts
```

核心类型：

```ts
type SourceLanguage = 'zh' | 'en' | 'ja';
type TargetLanguage = 'zh-CN' | 'en';
```

验收标准：

- 所有模块共用同一套语言类型。
- 不在各模块重复定义语言代码。

---

### 任务 3.2.2 实现 Pipeline

目标：

实现统一处理流程：

```text
AudioInput
  ↓
ASRProvider
  ↓
TranslatorProvider
  ↓
SubtitleSegment
```

交付物：

```text
packages/core/src/pipeline.ts
```

验收标准：

- Pipeline 可以使用 MockASRProvider 和 MockTranslator 跑通。
- Web Demo 可以调用 Pipeline。

---

## 3.3 浏览器端 ASR

### 任务 3.3.1 调研并选择浏览器 ASR 实现

候选方案：

- Transformers.js + Whisper / Distil-Whisper
- whisper.cpp WASM
- onnxruntime-web

本任务目标不是最终优化，而是确定第一版可运行方案。

验收标准：

- 确定第一版 ASR 实现方案。
- 能在浏览器中加载一个轻量模型。
- 能对上传音频进行识别。
- 至少验证 `ja` 和 `en` 两种语言。
- 如条件允许，再验证普通话 `zh`。

---

### 任务 3.3.2 实现 BrowserASRProvider

目标：

- 用选定的浏览器 ASR 方案实现真实 ASR。
- 支持 sourceLang 参数。
- 输出识别文本。

交付物：

```text
packages/asr-browser/src/BrowserASRProvider.ts
```

验收标准：

- 用户选择 `zh / en / ja` 时，ASR Provider 能接收对应 sourceLang。
- 至少能稳定识别上传音频文件。
- 识别结果能进入 Pipeline。

---

## 3.4 Chrome Extension

### 任务 3.4.1 创建 Extension Skeleton

目标：

创建 Chrome Extension 基础结构。

交付物：

```text
apps/extension/
  manifest.json
  src/background/serviceWorker.ts
  src/popup/Popup.tsx
  src/options/Options.tsx
  src/content/contentScript.ts
```

验收标准：

- 插件可以在 Chrome 中加载。
- Popup 可以打开。
- Options 页面可以打开。
- Content Script 可以注入页面。

---

### 任务 3.4.2 实现 Options 配置页

目标：

Options 页面支持配置：

- Source Language：zh / en / ja
- Target Language：zh-CN / en
- API Base URL
- API Key
- Model Name
- 是否显示源语言文本
- 字幕字号
- 字幕位置

配置存储：

```text
chrome.storage.local
```

验收标准：

- 配置可以保存。
- 插件重启后配置仍然存在。
- API Key 由用户输入，不写死。

---

### 任务 3.4.3 实现 Popup 控制

目标：

Popup 提供：

- Start
- Stop
- 当前状态
- 当前语言方向

验收标准：

- 点击 Start 可以通知 background 开始流程。
- 点击 Stop 可以通知 background 停止流程。
- Popup 显示当前 sourceLang / targetLang。

---

### 任务 3.4.4 实现字幕 Overlay

目标：

Content Script 在页面注入字幕层。

交付物：

```text
apps/extension/src/content/overlay.ts
apps/extension/src/content/overlay.css
```

验收标准：

- 字幕可以显示在页面底部。
- 支持显示 sourceText 和 translatedText。
- 支持基本样式配置。
- 可以接收 background 发来的字幕消息。

---

### 任务 3.4.5 实现 Tab Audio Capture

目标：

使用 `chrome.tabCapture` 捕获当前标签页音频。

交付物：

```text
apps/extension/src/offscreen/offscreen.ts
apps/extension/src/audio-worklet/captureProcessor.ts
```

验收标准：

- 点击 Start 后能捕获当前标签页音频。
- 捕获后用户仍能听到直播声音。
- AudioWorklet 能输出音频 chunk。
- Debug 日志能显示音频 chunk 状态。

---

### 任务 3.4.6 Extension 接入 Pipeline

目标：

把 tab 音频接入核心 Pipeline。

数据流：

```text
tab audio chunk
  ↓
BrowserASRProvider
  ↓
OpenAICompatibleTranslator
  ↓
Subtitle Overlay
```

验收标准：

- 在 Twitch / YouTube 页面点击 Start 后，能看到字幕结果。
- sourceLang 和 targetLang 使用用户配置。
- 翻译 API 使用用户配置的 OpenAI-compatible 参数。

---

## 4. 推荐文件结构

```text
vtuber-live-translator/
  README.md
  TARGET.md
  任务计划书.md
  package.json
  pnpm-workspace.yaml
  tsconfig.base.json

  apps/
    web-demo/
      package.json
      index.html
      vite.config.ts
      src/
        main.tsx
        App.tsx
        components/
          AudioUploader.tsx
          LanguageSelector.tsx
          SettingsPanel.tsx
          SubtitlePreview.tsx
          DebugPanel.tsx

    extension/
      package.json
      vite.config.ts
      manifest.json
      public/
        icons/
      src/
        background/
          serviceWorker.ts
          messageRouter.ts
        popup/
          Popup.tsx
          popup.html
        options/
          Options.tsx
          options.html
        content/
          contentScript.ts
          overlay.ts
          overlay.css
        offscreen/
          offscreen.html
          offscreen.ts
        audio-worklet/
          captureProcessor.ts

  packages/
    shared/
      package.json
      src/
        language.ts
        audio.ts
        asr.ts
        translation.ts
        subtitle.ts
        settings.ts
        messages.ts
        index.ts

    core/
      package.json
      src/
        pipeline.ts
        index.ts

    asr-browser/
      package.json
      src/
        BrowserASRProvider.ts
        MockASRProvider.ts
        audioUtils.ts
        modelRegistry.ts
        index.ts

    translator/
      package.json
      src/
        OpenAICompatibleTranslator.ts
        MockTranslator.ts
        prompt.ts
        index.ts

    subtitle/
      package.json
      src/
        SubtitleStore.ts
        subtitleTiming.ts
        index.ts
```

---

## 5. 阶段验收标准

## 5.1 Web Demo 验收

完成后应满足：

1. 可以选择 sourceLang：zh / en / ja。
2. 可以选择 targetLang：zh-CN / en。
3. 可以上传音频文件。
4. 可以使用 Mock ASR / Mock Translator 跑通流程。
5. 可以使用真实 OpenAI-compatible API 翻译文本。
6. 可以显示源文本和翻译文本。

---

## 5.2 Extension Skeleton 验收

完成后应满足：

1. 插件可以加载到 Chrome。
2. Popup 可以 Start / Stop。
3. Options 可以保存 API 配置和语言配置。
4. Content Script 可以显示假字幕。

---

## 5.3 完整 MVP 验收

完成后应满足：

1. 在 Twitch / YouTube 页面点击 Start。
2. 插件捕获当前标签页音频。
3. 浏览器 ASR 根据用户选择的 sourceLang 识别文本。
4. LLM 根据用户选择的 targetLang 翻译文本。
5. 页面 Overlay 显示翻译字幕。
6. 不需要 Python、Docker、WSL2 或本地服务。
7. API Key 由用户输入并保存在本地。

---

## 6. 当前阶段不做的事项

以下功能暂缓：

- 自动语言检测。
- 术语表。
- 韩语识别。
- 日文 / 韩文翻译输出。
- 平台字幕抓取。
- 本地 ASR Server。
- 云端 ASR。
- 多人说话人分离。
- AI 配音。
- 字幕导出。
- 账号系统。

---

## 7. 开发原则

1. 先跑通 Mock Pipeline，再接真实 ASR。
2. 先做 Web Demo，再做 Chrome Extension。
3. 所有语言配置使用统一类型定义。
4. LLM API 统一使用 OpenAI-compatible 格式。
5. API Key 不写死在代码中，只由用户输入。
6. ASR 和 Translator 都使用 Provider 抽象，方便后续替换。
7. 第一阶段只保证架构支持普通话、英语、日语识别，以及中文、英文输出。
8. 每个阶段都要有可运行结果。

